object | variable | Variable the termination condition is associated to. | m
+ Variable

numeric | lowerLimit | The propagation will terminate if the value of :key:`variable` is smaller than this value (or larger than :key:`upperLimit` if defined). | m:! upperLimit

numeric | upperLimit | The propagation will terminate if the value of :key:`variable` is larger than this value (or smaller than :key:`lowerLimit` if defined). | m:! lowerLimit
